[package]
name        = "graphite-geometry"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Graphite path geometry: path types, lyon fill/stroke tessellation, and cull-grade bounds (ADR-031/032)"

[dependencies]
lyon_path         = { workspace = true }
lyon_tessellation = { workspace = true }

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name    = "geometry"
harness = false
