{
  "name": "@graphite/plugin-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Graphite sandboxed plugin API. Phase 0: typed stub.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@graphite/protocol": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}
